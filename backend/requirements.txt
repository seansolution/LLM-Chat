fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30
alembic==1.13.1

# Vector / Search
qdrant-client==1.9.1
opensearch-py[async]==2.6.0

# NATS
nats-py==2.7.2

# HTTP
httpx==0.27.0
aiofiles==23.2.1
python-multipart==0.0.9

# Auth / JWT
# cryptography requires Rust on arm64 — installed via cargo in Dockerfile
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Observability
langfuse==2.28.0
prometheus-fastapi-instrumentator==7.0.0
opentelemetry-sdk==1.24.0

# Document parsing
pypdf==4.2.0
python-docx==1.1.2
markdown-it-py==3.0.0

# Text processing
# tiktoken has arm64 wheels on PyPI since 0.6.0
tiktoken==0.7.0
# Use nltk for chunking instead of langchain (lighter, pure Python, no Rust deps)
nltk==3.8.1

# HugeGraph REST client
requests==2.32.2
